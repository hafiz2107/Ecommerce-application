import type { Calculator, Dinero, DineroOptions } from '../types';
export declare type CreateDineroOptions<TAmount> = {
    readonly calculator: Calculator<TAmount>;
    readonly onCreate?: (options: DineroOptions<TAmount>) => void;
};
export declare function createDinero<TAmount>({ calculator, onCreate, }: CreateDineroOptions<TAmount>): ({ amount, currency, scale, }: DineroOptions<TAmount>) => Dinero<TAmount>;
