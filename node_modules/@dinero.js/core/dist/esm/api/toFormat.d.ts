import type { Calculator, Dinero, Transformer } from '../types';
export declare type ToFormatParams<TAmount> = readonly [
    dineroObject: Dinero<TAmount>,
    transformer: Transformer<TAmount>
];
export declare function toFormat<TAmount>(calculator: Calculator<TAmount>): (dineroObject: Dinero<TAmount>, transformer: Transformer<TAmount>) => string;
