import type { Calculator, Dinero, RoundingOptions } from '../types';
export declare type ToUnitParams<TAmount> = readonly [
    dineroObject: Dinero<TAmount>,
    options?: RoundingOptions<TAmount>
];
export declare function toUnit<TAmount>(calculator: Calculator<TAmount>): (dineroObject: Dinero<TAmount>, options?: RoundingOptions<TAmount> | undefined) => number;
